# A Tiny Radio

## Technical info (don't worry about it!!!)

### Encoding and deploying

After editing `reasons.json` locally, run:

```bash
python3 tools/encode.py
```

This overwrites `data.js` with the encoded content.
